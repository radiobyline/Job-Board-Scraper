{
  "name": "job-board-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Phase 1 jobs URL discovery + ATS classification pipeline for Ontario municipalities and First Nations",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "discovery": "tsx src/discovery.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  }
}
